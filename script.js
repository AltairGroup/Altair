const area=document.querySelector('#area');const total=document.querySelector('#total');const buttons=[...document.querySelectorAll('.toggle button')];let rate=69000;function calc(){if(!area||!total)return;const a=Math.max(0,Number(area.value)||0);total.textContent=new Intl.NumberFormat('ru-RU').format(a*rate)+' ₸'}if(area){area.addEventListener('input',calc)}buttons.forEach(b=>b.addEventListener('click',()=>{buttons.forEach(x=>x.classList.remove('active'));b.classList.add('active');rate=Number(b.dataset.rate);calc()}));calc();
const form=document.querySelector('#contactForm');if(form){form.addEventListener('submit',e=>{e.preventDefault();const name=form.elements.name.value.trim();const phone=form.elements.phone.value.trim();const areaValue=form.elements.area.value.trim();const text=`Здравствуйте! Хочу обсудить ремонт.%0AИмя: ${encodeURIComponent(name)}%0AТелефон: ${encodeURIComponent(phone)}%0AПлощадь: ${encodeURIComponent(areaValue||'не указана')} м²`;window.open(`https://wa.me/77073749855?text=${text}`,'_blank','noopener')})}
const burger=document.querySelector('.burger');const nav=document.querySelector('.nav nav');if(burger&&nav){burger.addEventListener('click',()=>{nav.classList.toggle('mobile-open')})}